---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: platform
  namespace: flux-system
spec:
  interval: 1h
  retryInterval: 2m0s
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  path: ./fluxcd/platform/production
  prune: true
  wait: true
  timeout: 10m0s
  dependsOn:
    - name: infrastructure
      namespace: flux-system
    - name: databases
      namespace: flux-system
  decryption:
    provider: sops
    secretRef:
      name: sops-age
